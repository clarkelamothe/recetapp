<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <ImageButton
        android:id="@+id/ib_error"
        android:layout_width="100dp"
        android:layout_height="100dp"
        android:background="@color/white"
        android:contentDescription="@string/error_image_description"
        android:scaleType="fitCenter"
        android:src="@drawable/baseline_error_24"
        android:visibility="visible"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent" />

    <TextView
        android:id="@+id/tv_error_msg"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="16dp"
        android:text="@string/tap_to_retry"
        android:visibility="visible"
        app:layout_constraintEnd_toEndOf="@+id/ib_error"
        app:layout_constraintStart_toStartOf="@+id/ib_error"
        app:layout_constraintTop_toBottomOf="@+id/ib_error" />
</androidx.constraintlayout.widget.ConstraintLayout>